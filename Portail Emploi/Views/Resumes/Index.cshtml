@model IEnumerable<Portail_Emploi.Models.Resume>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Candidats)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Candidats.Nom)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Resume">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Resume">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Resume">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
