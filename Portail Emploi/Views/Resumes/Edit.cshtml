@model Portail_Emploi.Models.Resume

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Resume</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ID_Resume" />
            <div class="form-group">
                <label asp-for="ID_Candidat" class="control-label"></label>
                <select asp-for="ID_Candidat" class="form-control" asp-items="ViewBag.ID_Candidat"></select>
                <span asp-validation-for="ID_Candidat" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
